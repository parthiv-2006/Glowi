import { Screen, AppText } from '@/components/ui';

// Implemented in the learn-library task.
export default function LearnScreen() {
  return (
    <Screen>
      <AppText variant="display">Learn</AppText>
    </Screen>
  );
}
