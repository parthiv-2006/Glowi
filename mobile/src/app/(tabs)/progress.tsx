import { Screen, AppText } from '@/components/ui';

// Implemented in the progress/streaks task.
export default function ProgressScreen() {
  return (
    <Screen>
      <AppText variant="display">Progress</AppText>
    </Screen>
  );
}
