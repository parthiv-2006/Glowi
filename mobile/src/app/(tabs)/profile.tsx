import { Screen, AppText } from '@/components/ui';

// Implemented alongside the chatbot + memory task.
export default function ProfileTab() {
  return (
    <Screen>
      <AppText variant="display">Profile</AppText>
    </Screen>
  );
}
