import { Screen, AppText } from '@/components/ui';

// Implemented in the chatbot + memory task.
export default function ChatTab() {
  return (
    <Screen>
      <AppText variant="display">Coach</AppText>
    </Screen>
  );
}
